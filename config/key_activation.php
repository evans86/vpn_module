<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Событие успешной активации (KeyActivated)
    |--------------------------------------------------------------------------
    | Диспатчится после успешной активации; слушатель может отправить письмо на key_activate.email.
    */
    'events' => [
        'enabled' => env('KEY_ACTIVATION_EVENTS_ENABLED', true),
    ],

    /*
    |--------------------------------------------------------------------------
    | Уведомление на email при активации
    |--------------------------------------------------------------------------
    | stub_log: для отладки цепочки пишет в лог без реальной отправки почты.
    | Реальная отправка будет добавлена отдельно (Mailable и т.д.).
    */
    'email' => [
        'stub_log' => env('KEY_ACTIVATION_EMAIL_STUB_LOG', false),
    ],

];
