<?php

return [
    /*
    |--------------------------------------------------------------------------
    | BOT-T Integration Settings
    |--------------------------------------------------------------------------
    |
    | Настройки интеграции с BOT-T для автоматической выдачи ключей
    | после оплаты заказов
    |
    */

    /*
    |--------------------------------------------------------------------------
    | Pack Mapping
    |--------------------------------------------------------------------------
    |
    | Маппинг между API ID категории из BOT-T и ID пакета в VPN системе
    | 
    | Формат: 'api_id' => 'pack_id'
    | Пример: [123 => 1, 456 => 2]
    |
    | Если пакет имеет поле api_id, маппинг не обязателен
    |
    */
    'pack_mapping' => env('BOTT_PACK_MAPPING', '') 
        ? json_decode(env('BOTT_PACK_MAPPING', '{}'), true) 
        : [],

    /*
    |--------------------------------------------------------------------------
    | Auto Create Salesman
    |--------------------------------------------------------------------------
    |
    | Автоматически создавать salesman для пользователей из BOT-T,
    | если они не существуют в системе
    |
    */
    'auto_create_salesman' => env('BOTT_AUTO_CREATE_SALESMAN', true),

    /*
    |--------------------------------------------------------------------------
    | Default Salesman Token
    |--------------------------------------------------------------------------
    |
    | Токен бота для новых salesman, созданных из BOT-T
    | По умолчанию используется токен Father Bot
    |
    */
    'default_salesman_token' => env('BOTT_DEFAULT_SALESMAN_TOKEN', ''),
];

