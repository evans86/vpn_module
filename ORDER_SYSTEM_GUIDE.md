# Система полуавтоматической продажи пакетов

## Описание

Система позволяет продавцам покупать пакеты ключей через Telegram-бота с полуавтоматической обработкой заказов администратором.

## Установка

**⚠️ ВАЖНО: Перед установкой создайте резервную копию базы данных!**

1. Выполните миграции:
```bash
php artisan migrate
```

2. Создайте симлинк для хранения изображений (если еще не создан):
```bash
php artisan storage:link
```

3. Создайте директорию для подтверждений оплаты:
```bash
mkdir -p storage/app/public/order_proofs
chmod -R 755 storage/app/public/order_proofs
```

4. Очистите кэш:
```bash
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

**📋 Подробное руководство по развертыванию на продакшн см. в файле `DEPLOYMENT_GUIDE.md`**

## Настройка системы

### 1. Включение/выключение системы

1. Зайдите в админ-панель → **Заказы** → **Настройки системы заказов**
2. Включите/выключите систему с помощью переключателя
3. Укажите Telegram ID администратора для получения уведомлений о новых заказах (необязательно)

### 2. Настройка доступных пакетов

1. В разделе **Настройки системы заказов** выберите, какие пакеты будут доступны для покупки
2. Установите порядок отображения пакетов (чем меньше число, тем выше в списке)
3. Сохраните настройки

### 3. Настройка способов оплаты

1. Зайдите в **Заказы** → **Способы оплаты**
2. Добавьте способы оплаты (банковские карты, криптовалюты, электронные кошельки и т.д.)
3. Укажите реквизиты для перевода и инструкции для пользователей
4. Установите порядок отображения и статус (активен/неактивен)

## Использование для продавцов

### Процесс покупки пакета:

1. Продавец заходит в FatherBot и нажимает кнопку **🛒 Купить пакет**
2. Выбирает нужный пакет из списка
3. Выбирает способ оплаты
4. Получает реквизиты для оплаты
5. Отправляет скриншот подтверждения оплаты (фото чека или перевода)
6. Ожидает проверки администратором
7. После одобрения получает пакет с ключами

## Работа администратора

### Просмотр заказов:

1. Зайдите в **Заказы** → **Список заказов**
2. Используйте фильтры для поиска нужных заказов
3. Нажмите **Просмотр** для детальной информации о заказе

### Обработка заказа:

1. Откройте заказ со статусом **"Ожидает подтверждения"**
2. Просмотрите подтверждение оплаты (скриншот)
3. Проверьте соответствие суммы и реквизитов
4. Выберите действие:
   - **Одобрить заказ** - пакет будет автоматически выдан продавцу
   - **Отклонить заказ** - укажите причину отклонения (необязательно)

### Управление способами оплаты:

1. Зайдите в **Заказы** → **Способы оплаты**
2. Добавьте, отредактируйте или удалите способы оплаты
3. Настройте порядок отображения и статус

## Статусы заказов

- **Ожидает оплаты** - заказ создан, ожидается оплата
- **Ожидает подтверждения** - оплата отправлена, ожидается проверка администратором
- **Одобрен** - заказ одобрен, пакет выдан продавцу
- **Отклонен** - заказ отклонен администратором
- **Отменен** - заказ отменен продавцом

## Интеграция с существующей системой

Система полностью интегрирована с существующими компонентами:
- Использует существующие модели `Pack` и `PackSalesman`
- Автоматически создает `PackSalesman` при одобрении заказа
- Использует существующий `PackSalesmanService` для выдачи пакетов
- Не нарушает текущую работу системы

## Важные замечания

1. **Хранение изображений**: Убедитесь, что создан симлинк `storage/app/public` → `public/storage`
2. **Права доступа**: Убедитесь, что директория `storage/app/public/order_proofs` имеет права на запись
3. **Уведомления**: Для получения уведомлений о новых заказах укажите Telegram ID администратора в настройках

